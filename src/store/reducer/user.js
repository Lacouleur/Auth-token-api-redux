// - initialState
const initialState = {
    username: '',
    avatar: '',
    logged: false,
  };
  
  // - Actions Types
  
  // - Reducer
  const reducer = (state = initialState, action = {}) => {
    switch (action.type) {
      default:
        return state;
    }
  };
  
  // - Actions Creators
  
  // - Selectors
  
  // - Export
  export default reducer;